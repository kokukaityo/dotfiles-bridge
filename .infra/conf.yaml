# パス設定
path:
    dotfile: ..
    sync: ./conf.yaml

# インフラフォルダ
.infra:
    sync: manual

# 同期フォルダ
ai-agent:
    sync: auto
editor:
    sync: auto
shell:
    sync: auto

# 一時保管フォルダ
backup:
    sync: ignore
raw:
    sync: ignore
